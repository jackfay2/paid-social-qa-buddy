[pytest]
pythonpath = .
# Only collect the worker's tests here. listener/ is a vendored copy of Maya's
# listener that reuses the `app` package name, so it's tested separately
# (cd listener && pytest) to avoid an import clash with our worker's app/.
testpaths = tests
