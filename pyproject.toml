[project]
name = "paid-social-qa-buddy"
version = "0.1.0"
description = "Paid Social QA Buddy Bot — Meta worker (Polaris + BigQuery + Gemini)"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP"]
ignore = ["E501"]
